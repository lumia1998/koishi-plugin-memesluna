# 更新日志

## [0.6.0-alpha.1] - 2026-07-08

### 💥 破坏性变更

- **移除同义词组功能**：删除 `synonymGroups` 和 `enableEmotionTags` 配置项
  - 原因：配置复杂，用户难以理解和维护
  - 影响：现有配置文件需要删除这两个配置项

- **移除标签路由**：不再支持 `/memesluna/:tag` 独立路由
  - 原因：标签应作为内部索引，不作为外部访问入口
  - 替代：使用全局搜索 `/memesluna?q=标签名`

- **移除标签视图**：控制台不再提供标签分类浏览功能
  - 原因：简化 UI，降低用户认知负担
  - 替代：使用搜索功能查找图片

- **移除聊天侧创建合集命令**：不再注册 `memesluna.add` / `memesluna.create` / `memesluna.creat`
  - 原因：合集创建统一在 Koishi Console 的 MemesLuna 页面完成
  - 影响：`memesluna.stole` 只会保存到已有合集，不再自动创建合集

### ✨ 新增与优化

- **搜索精准度提升**：改为"最高分优先（并列随机）"策略，搜索结果更匹配用户意图
- **AI 标注优化**：移除同义词约束，基于图像内容自由标注
- **自动标注触发简化**：配置 ChatLuna 模型后，上传、偷图和暂缓区归档会自动触发标注，不再需要 `autoAnnotate` 开关
- **前端结构拆分**：将 Console 管理页拆分为布局、合集、端点、暂缓区、预览等组件，降低维护成本
- **标签数量调整**：单张图片标签从 8 个减少到 5 个，更聚焦核心语义
- **代码量减少 17%**：移除 ~1200 行代码，提升可维护性
- **配置项简化**：移除 `enableEmotionTags`、`synonymGroups` 和 `autoAnnotate`

### 📖 迁移指南

1. 删除配置文件中的 `enableEmotionTags` 配置项
2. 删除配置文件中的 `synonymGroups` 配置项
3. 删除配置文件中的 `autoAnnotate` 配置项；配置 `model` 后会自动标注新入库图片
4. 已有的图片 `tags` 和 `aliases` 数据无需修改
5. 标签路由 → 使用全局搜索 `/memesluna?q=标签名` 替代
6. 标签视图 → 使用搜索功能替代
7. 合集创建 → 使用 Koishi Console 的 MemesLuna 页面替代 `memesluna.add`

### ⚠️ 注意事项

- **不兼容 v0.5.x 配置**：升级前必须删除废弃的配置项
- 建议在测试环境验证后再升级生产环境

## [0.5.10] - 2026-07-05

### ✨ 新增与优化
- **情感标签开关**：新增 `enableEmotionTags`，默认关闭。开启后才启用 `/标签名` 跨合集路由与 `{tag_routes}` 提示词注入。
- **标签提示词防误注入**：仅注入已有图片实际使用过的标签组，避免 ChatLuna 获得不存在的 `/memesluna/标签名` 路径。
- **固定存储目录**：移除 `storagePath` 配置项，表情包固定保存到 Koishi 数据目录下的 `data/memesluna`。
- **重复上传保留标注**：重复上传相同图片仍覆盖旧图，但保留旧图的 `tags` 与 `aliases`。
- **标签路由缓存刷新**：图片、标签、外链、合集等数据更新后会主动刷新标签路由缓存。
- **偷图容错优化**：批量偷图时遇到不兼容格式会跳过并汇总提示，不再中断已成功保存的图片。
- **README 完善**：补充 Console 用法、路由说明、合集搜索、情感标签、ChatLuna 注入、AI 标注、暂缓区和命令说明。

## [0.5.9] - 2026-06-30

### ✨ 修复与优化
- **端点名称支持中文**：修改了端点路由正则表达式限制，允许端点名称包含中文，并同步更新了错误提示信息。
- **文件上传解析优化**：增强了 HTTP 文件上传时的多部分表单解析逻辑，优先检测并使用上游中间件已解析的文件对象，并支持更多上传参数字段（如 `file`、`files`、`images` 等）。
- **上传限制提升**：将单次最多选择上传的表情图片数量限制从 50 张提升至 500 张。
