# 更新日志

## [0.5.10] - 2026-07-05

### ✨ 新增与优化
- **情感标签开关**：新增 `enableEmotionTags`，默认关闭。开启后才启用 `/标签名` 跨合集路由与 `{tag_routes}` 提示词注入。
- **标签提示词防误注入**：仅注入已有图片实际使用过的标签组，避免 ChatLuna 获得不存在的 `/memesluna/标签名` 路径。
- **固定存储目录**：移除 `storagePath` 配置项，表情包固定保存到 Koishi 数据目录下的 `data/memesluna`。
- **重复上传保留标注**：重复上传相同图片仍覆盖旧图，但保留旧图的 `tags` 与 `aliases`。
- **标签路由缓存刷新**：图片、标签、外链、合集等数据更新后会主动刷新标签路由缓存。
- **偷图容错优化**：批量偷图时遇到不兼容格式会跳过并汇总提示，不再中断已成功保存的图片。
- **README 完善**：补充 Console 用法、路由说明、合集搜索、情感标签、ChatLuna 注入、AI 标注、暂缓区和命令说明。

## [0.5.9] - 2026-06-30

### ✨ 修复与优化
- **端点名称支持中文**：修改了端点路由正则表达式限制，允许端点名称包含中文，并同步更新了错误提示信息。
- **文件上传解析优化**：增强了 HTTP 文件上传时的多部分表单解析逻辑，优先检测并使用上游中间件已解析的文件对象，并支持更多上传参数字段（如 `file`、`files`、`images` 等）。
- **上传限制提升**：将单次最多选择上传的表情图片数量限制从 50 张提升至 500 张。
